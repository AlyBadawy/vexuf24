<!DOCTYPE html>
<html class="dark">
  <head>
    <title>Vexuf 24</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag resolve_layout, "data-turbo-track": "reload", type: "module" %>
  </head>
  <body>
    <%= turbo_frame_tag :cookies_banner, src: cookies_banner_path if session[:cookies_accepted].nil? %>
    <% if resolve_layout == "application" %>
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
    <% end %>
    <%= yield %>
    <% if resolve_layout == "application" %>
      <div id="theme-provider" >
      </div>
    <% end %>
  </body>
</html>        